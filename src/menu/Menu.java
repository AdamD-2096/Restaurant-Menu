package menu;

import java.util.Date;
import java.util.HashMap;

public class Menu {
    private HashMap<String, MenuItem> appitizerItems = new HashMap<>();
    private HashMap<String, MenuItem> mainCourseItems = new HashMap<>();
    private HashMap<String, MenuItem> DessertItems = new HashMap<>();
    private Date date = new Date();

}
